[build]
builder = "NIXPACKS"
buildCommand = "npm install && npm run build --workspace=@artgenesis/api"

[deploy]
startCommand = "npm run start:prod --workspace=@artgenesis/api"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
